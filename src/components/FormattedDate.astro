---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('es-es', {
			year: 'numeric',
			month: 'short',
			day: '2-digit',
			timeZone: 'UTC'
		})
	}
</time>
